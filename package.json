{
  "name": "atlanta-code-camp-2019",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress": "cypress run --config baseUrl=http://localhost:4200",
    "cypress:open": "cypress open --config baseUrl=http://localhost:4200",
    "cypress:prod": "cypress open --config baseUrl=http://matthewyknowles.com"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.4.1",
    "cypress-image-snapshot": "^3.1.1"
  }
}
